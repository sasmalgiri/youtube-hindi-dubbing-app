fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
edge-tts==7.2.7
faster-whisper==1.1.1
deep-translator==1.11.4
google-genai>=1.0.0
groq>=0.9.0
openai>=1.0.0
elevenlabs>=2.0.0
google-cloud-texttospeech>=2.16.0
requests>=2.28.0
sse-starlette==2.1.0
rich==13.9.4
yt-dlp>=2024.1.0
pywebview>=5.0
supabase>=2.0.0

# ── GPU features: WhisperX word-level alignment + Demucs background separation ──
# torch must be the CUDA build to use the GPU. On an NVIDIA machine install with:
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cu121
# (plain `pip install torch` pulls the CPU build — WhisperX/Demucs still run, just slowly.)
torch>=2.2
whisperx>=3.8.0        # forced alignment for tighter word timestamps (on by default)
demucs>=4.0.0          # vocal/background stem separation for "keep background music"
